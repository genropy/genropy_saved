.. _genro_readonly:

========
readOnly
========

    ::
    
        readOnly = True
        readOnly = False
        
    Boolean. If ``True``, the element that carry the readOnly attribute is
    in read-only mode.
    
    **Validity:** it works on every object.